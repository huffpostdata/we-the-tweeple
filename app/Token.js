'use strict'

module.exports = class Token {
  constructor(token, nTotal, nClinton, nTrump, png) {
    this.token = token
    this.nTotal = nTotal
    this.nClinton = nClinton
    this.nTrump = nTrump
    this.png = png
  }
}
