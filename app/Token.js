'use strict'

module.exports = class Token {
  constructor(token, nTotal, nClinton, nTrump, image) {
    this.token = token
    this.nTotal = nTotal
    this.nClinton = nClinton
    this.nTrump = nTrump
    this.image = image
  }
}
