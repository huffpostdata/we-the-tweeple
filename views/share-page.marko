<!DOCTYPE html>

html lang="en"
  head
    meta charset="utf-8"
    title - TK
    meta name="viewport" content="width=device-width, initial-scale=1"
  body
    p
      img width="1000" height="700" src="data:image/png;base64,${data.model.renderPng()}"

    p - TK put this image in the metadata, as a separate URL
    p - TK make this page a "bootstrap" page: a page we'll never change that loads the main page and its JS....
