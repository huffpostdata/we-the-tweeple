header data-interaction-grid-path="${data.path_to_asset('digest', 'data/desktop-birds-grid.json')}"
  h1 - ${data.model.metadata.title}
  figure
  h2 - ${data.model.metadata.dek}
  .authors - $!{data.model.metadata.author_html}
  .published-at - Published <time>${data.model.metadata.date_published}</time>
